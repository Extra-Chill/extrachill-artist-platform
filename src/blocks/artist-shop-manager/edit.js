import { __ } from '@wordpress/i18n';

export default function Edit() {
	return (
		<div className="ec-artist-shop-manager">
			<p>{ __( 'Artist Shop Manager renders on the frontend.', 'extrachill-artist-platform' ) }</p>
		</div>
	);
}
